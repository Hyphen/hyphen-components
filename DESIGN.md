---
version: "alpha"
name: "Hyphen"
description: "The Hyphen design system. Tokens are generated from @hyphen/hyphen-design-tokens; components are implemented by @hyphen/hyphen-components."
colors:
  primary: "#171717"
  primary-dark: "#FFFFFF"
  on-primary: "#FFFFFF"
  on-primary-dark: "#525252"
  secondary: "#FFFFFF"
  secondary-dark: "#171717"
  on-secondary: "#737373"
  on-secondary-dark: "#FFFFFF"
  surface: "#FFFFFF"
  surface-dark: "#262626"
  on-surface: "#404040"
  on-surface-dark: "#f5f5f5"
  surface-secondary: "#f5f5f5"
  surface-secondary-dark: "#171717"
  on-surface-secondary: "#737373"
  on-surface-secondary-dark: "#d4d4d4"
  surface-tertiary: "#e5e5e5"
  surface-tertiary-dark: "#0a0a0a"
  on-surface-tertiary: "#a3a3a3"
  surface-inverse: "#262626"
  surface-inverse-dark: "#e5e5e5"
  on-surface-inverse: "#e5e5e5"
  on-surface-inverse-dark: "#404040"
  surface-disabled: "#e5e5e5"
  surface-disabled-dark: "#0a0a0a"
  on-surface-disabled: "#a3a3a3"
  on-surface-disabled-dark: "#525252"
  placeholder: "#a3a3a3"
  placeholder-dark: "#737373"
  outline: "#d4d4d4"
  outline-dark: "#525252"
  outline-subtle: "#e5e5e5"
  outline-subtle-dark: "#404040"
  outline-hover: "#a3a3a3"
  outline-hover-dark: "#404040"
  outline-active: "#737373"
  outline-active-dark: "#262626"
  error: "#ef4444"
  surface-danger: "#fef2f2"
  on-danger: "#dc2626"
  on-danger-dark: "#ef4444"
  outline-danger: "#fca5a5"
  outline-danger-dark: "#fecaca"
  surface-success: "#f0fdf4"
  on-success: "#15803d"
  on-success-dark: "#16a34a"
  outline-success: "#bbf7d0"
  surface-warning: "#fefce8"
  on-warning: "#ca8a04"
  on-warning-dark: "#eab308"
  outline-warning: "#fde047"
  outline-warning-dark: "#fef08a"
  surface-info: "#f0f9ff"
  on-info: "#2563eb"
  on-info-dark: "#60a5fa"
  outline-info: "#bfdbfe"
  chart-1: "#1d4ed8"
  chart-2: "#93c5fd"
  chart-3: "#3b82f6"
  button-primary-active-background: "#171717"
  button-primary-active-background-dark: "#e5e5e5"
  button-secondary-hover-background: "#f5f5f5"
  button-secondary-hover-background-dark: "#262626"
  button-secondary-hover-font: "#525252"
  button-secondary-hover-font-dark: "#FFFFFF"
  button-secondary-hover-border: "#d4d4d4"
  button-secondary-hover-border-dark: "#737373"
  button-secondary-active-background: "#e5e5e5"
  button-secondary-active-background-dark: "#171717"
  button-tertiary-background: "transparent"
  button-danger-font: "#FFFFFF"
  button-danger-hover-background: "#dc2626"
  button-danger-active-background: "#b91c1c"
  button-success-background: "#15803d"
  button-success-hover-background: "#166534"
  button-success-active-background: "#12622f"
  badge-secondary-border: "#e5e5e5"
  badge-secondary-border-dark: "#262626"
  badge-danger-background: "#fef2f2"
  badge-danger-background-dark: "#7f1d1d"
  badge-danger-font: "#dc2626"
  badge-danger-font-dark: "#f87171"
  badge-danger-border: "#fee2e2"
  badge-danger-border-dark: "#991b1b"
  badge-outline-border: "#d4d4d4"
  badge-outline-border-dark: "#404040"
  toast-background: "#262626"
  toast-background-dark: "#f5f5f5"
  toast-font: "#f5f5f5"
  toast-font-dark: "#404040"
  brand-yellow-background: "#ffd200"
  brand-orange-background: "#ff911e"
  brand-magenta-background: "#ff3278"
  brand-dark-grey-background: "#323237"
  brand-cyan-background: "#00b4ff"
  brand-pink-background: "#dc50ff"
  brand-light-purple-background: "#b47dff"
  brand-medium-purple-background: "#4b32aa"
  brand-dark-purple-background: "#1e1e5a"
  checkbox-border: "#737373"
  checkbox-checked-background: "#404040"
  checkbox-checked-background-dark: "#FFFFFF"
typography:
  display-lg:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "4.5rem"
    fontWeight: 600
    lineHeight: 1.25
  display-md:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "4rem"
    fontWeight: 600
    lineHeight: 1.25
  display-sm:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "3.5rem"
    fontWeight: 600
    lineHeight: 1.25
  h1:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "3rem"
    fontWeight: 600
    lineHeight: 1.25
  h2:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "2.5rem"
    fontWeight: 600
    lineHeight: 1.25
  h3:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "2rem"
    fontWeight: 600
    lineHeight: 1.25
  h4:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "1.5rem"
    fontWeight: 600
    lineHeight: 1.25
  h5:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "1.25rem"
    fontWeight: 600
    lineHeight: 1.25
  h6:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "1rem"
    fontWeight: 600
    lineHeight: 1.25
  body-lg:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "1.125rem"
    fontWeight: 400
    lineHeight: 1.5
  body-md:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "1rem"
    fontWeight: 400
    lineHeight: 1.5
  body-sm:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: 1.5
  body-xs:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "0.75rem"
    fontWeight: 400
    lineHeight: 1.5
  label-md:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 500
    lineHeight: 1.25
  label-sm:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "0.75rem"
    fontWeight: 500
    lineHeight: 1.25
  input:
    fontFamily: "'geist', 'Source Sans', 'Helvetica Neue', Helvetica, 'Arial Black', Arial, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: 1.15
  code:
    fontFamily: "'geist-mono', ui-monospace, Menlo, Monaco, 'Cascadia Mono', 'Segoe UI Mono', 'Roboto Mono', 'Oxygen Mono', 'Ubuntu Monospace', 'Source Code Pro', 'Fira Mono', 'Droid Sans Mono', 'Courier New', monospace"
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: 1.5
rounded:
  "0": "0rem"
  xs: "0.125rem"
  sm: "0.25rem"
  md: "0.5rem"
  lg: "0.625rem"
  xl: "0.75rem"
  2xl: "1rem"
  3xl: "2rem"
spacing:
  "0": "0rem"
  2xs: "0.125rem"
  xs: "0.25rem"
  sm: "0.5rem"
  md: "0.625rem"
  lg: "0.75rem"
  xl: "1rem"
  2xl: "1.25rem"
  3xl: "1.5rem"
  4xl: "2rem"
  5xl: "4rem"
  6xl: "6rem"
  7xl: "8rem"
  8xl: "10rem"
  9xl: "12rem"
  10xl: "16rem"
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    borderColor: "{colors.primary}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-primary-dark:
    backgroundColor: "{colors.primary-dark}"
    textColor: "{colors.on-primary-dark}"
    borderColor: "{colors.primary-dark}"
  button-primary-hover:
    backgroundColor: "{colors.surface-inverse}"
    textColor: "{colors.on-primary}"
    borderColor: "{colors.surface-inverse}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-primary-hover-dark:
    backgroundColor: "{colors.surface-inverse-dark}"
    textColor: "{colors.on-primary-dark}"
    borderColor: "{colors.surface-inverse-dark}"
  button-primary-active:
    backgroundColor: "{colors.button-primary-active-background}"
    textColor: "{colors.on-primary}"
    borderColor: "{colors.button-primary-active-background}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-primary-active-dark:
    backgroundColor: "{colors.button-primary-active-background-dark}"
    textColor: "{colors.on-primary-dark}"
    borderColor: "{colors.button-primary-active-background-dark}"
  button-secondary:
    backgroundColor: "{colors.secondary}"
    textColor: "{colors.on-secondary}"
    borderColor: "{colors.outline}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-secondary-dark:
    backgroundColor: "{colors.secondary-dark}"
    textColor: "{colors.on-secondary-dark}"
    borderColor: "{colors.outline-dark}"
  button-secondary-hover:
    backgroundColor: "{colors.button-secondary-hover-background}"
    textColor: "{colors.button-secondary-hover-font}"
    borderColor: "{colors.button-secondary-hover-border}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-secondary-hover-dark:
    backgroundColor: "{colors.button-secondary-hover-background-dark}"
    textColor: "{colors.button-secondary-hover-font-dark}"
    borderColor: "{colors.button-secondary-hover-border-dark}"
  button-secondary-active:
    backgroundColor: "{colors.button-secondary-active-background}"
    textColor: "{colors.button-secondary-hover-font}"
    borderColor: "{colors.on-surface-tertiary}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-secondary-active-dark:
    backgroundColor: "{colors.button-secondary-active-background-dark}"
    textColor: "{colors.button-secondary-hover-font-dark}"
  button-tertiary:
    backgroundColor: "{colors.button-tertiary-background}"
    textColor: "{colors.on-secondary}"
    borderColor: "{colors.button-tertiary-background}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-tertiary-dark:
    textColor: "{colors.on-secondary-dark}"
  button-tertiary-hover:
    backgroundColor: "{colors.on-surface-inverse}"
    textColor: "{colors.button-secondary-hover-font}"
    borderColor: "{colors.button-tertiary-background}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-tertiary-hover-dark:
    backgroundColor: "{colors.on-surface-inverse-dark}"
    textColor: "{colors.button-secondary-hover-font-dark}"
  button-tertiary-active:
    backgroundColor: "{colors.outline}"
    textColor: "{colors.button-secondary-hover-font}"
    borderColor: "{colors.button-tertiary-background}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-tertiary-active-dark:
    backgroundColor: "{colors.outline-dark}"
    textColor: "{colors.button-secondary-hover-font-dark}"
  button-danger:
    backgroundColor: "{colors.error}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-danger-hover:
    backgroundColor: "{colors.button-danger-hover-background}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-danger-active:
    backgroundColor: "{colors.button-danger-active-background}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-success:
    backgroundColor: "{colors.button-success-background}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-success-hover:
    backgroundColor: "{colors.button-success-hover-background}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-success-active:
    backgroundColor: "{colors.button-success-active-background}"
    textColor: "{colors.button-danger-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  badge-default:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.secondary}"
    borderColor: "{colors.primary}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-sm}"
  badge-default-dark:
    backgroundColor: "{colors.primary-dark}"
    textColor: "{colors.secondary-dark}"
    borderColor: "{colors.primary-dark}"
  badge-secondary:
    backgroundColor: "{colors.on-surface-inverse}"
    textColor: "{colors.surface-inverse}"
    borderColor: "{colors.badge-secondary-border}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-sm}"
  badge-secondary-dark:
    backgroundColor: "{colors.on-surface-inverse-dark}"
    textColor: "{colors.surface-inverse-dark}"
    borderColor: "{colors.badge-secondary-border-dark}"
  badge-danger:
    backgroundColor: "{colors.badge-danger-background}"
    textColor: "{colors.badge-danger-font}"
    borderColor: "{colors.badge-danger-border}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-sm}"
  badge-danger-dark:
    backgroundColor: "{colors.badge-danger-background-dark}"
    textColor: "{colors.badge-danger-font-dark}"
    borderColor: "{colors.badge-danger-border-dark}"
  badge-outline:
    backgroundColor: "{colors.button-tertiary-background}"
    textColor: "{colors.surface-inverse}"
    borderColor: "{colors.badge-outline-border}"
    rounded: "{rounded.sm}"
    typography: "{typography.label-sm}"
  badge-outline-dark:
    textColor: "{colors.surface-inverse-dark}"
    borderColor: "{colors.badge-outline-border-dark}"
  toast:
    backgroundColor: "{colors.toast-background}"
    textColor: "{colors.toast-font}"
    borderColor: "{colors.button-primary-active-background}"
    rounded: "{rounded.md}"
    typography: "{typography.body-sm}"
    padding: "{spacing.xl}"
  toast-dark:
    backgroundColor: "{colors.toast-background-dark}"
    textColor: "{colors.toast-font-dark}"
    borderColor: "{colors.button-primary-active-background-dark}"
  toast-error:
    backgroundColor: "{colors.error}"
    textColor: "{colors.button-danger-font}"
    borderColor: "{colors.button-danger-hover-background}"
    rounded: "{rounded.md}"
    typography: "{typography.body-sm}"
    padding: "{spacing.xl}"
  tooltip:
    backgroundColor: "{colors.toast-background}"
    textColor: "{colors.toast-font}"
    rounded: "{rounded.sm}"
    typography: "{typography.body-xs}"
    padding: "{spacing.sm}"
  tooltip-dark:
    backgroundColor: "{colors.toast-background-dark}"
    textColor: "{colors.toast-font-dark}"
  popover:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.md}"
    typography: "{typography.body-sm}"
    padding: "{spacing.xl}"
  popover-dark:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.on-surface-dark}"
  brand-yellow:
    backgroundColor: "{colors.brand-yellow-background}"
  brand-orange:
    backgroundColor: "{colors.brand-orange-background}"
  brand-magenta:
    backgroundColor: "{colors.brand-magenta-background}"
  brand-dark-grey:
    backgroundColor: "{colors.brand-dark-grey-background}"
  brand-cyan:
    backgroundColor: "{colors.brand-cyan-background}"
  brand-pink:
    backgroundColor: "{colors.brand-pink-background}"
  brand-light-purple:
    backgroundColor: "{colors.brand-light-purple-background}"
  brand-medium-purple:
    backgroundColor: "{colors.brand-medium-purple-background}"
  brand-dark-purple:
    backgroundColor: "{colors.brand-dark-purple-background}"
  checkbox:
    borderColor: "{colors.checkbox-border}"
    rounded: "{rounded.xs}"
  checkbox-checked:
    backgroundColor: "{colors.checkbox-checked-background}"
    rounded: "{rounded.xs}"
  checkbox-checked-dark:
    backgroundColor: "{colors.checkbox-checked-background-dark}"
  page:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    typography: "{typography.body-md}"
  page-dark:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.on-surface-dark}"
  card:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    borderColor: "{colors.outline-subtle}"
    rounded: "{rounded.md}"
    padding: "{spacing.3xl}"
  card-dark:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.on-surface-dark}"
    borderColor: "{colors.outline-subtle-dark}"
  card-subtle:
    backgroundColor: "{colors.surface-secondary}"
    textColor: "{colors.on-surface-secondary}"
    rounded: "{rounded.md}"
    padding: "{spacing.3xl}"
  card-subtle-dark:
    backgroundColor: "{colors.surface-secondary-dark}"
    textColor: "{colors.on-surface-secondary-dark}"
  surface-sunken:
    backgroundColor: "{colors.surface-tertiary}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.md}"
  surface-sunken-dark:
    backgroundColor: "{colors.surface-tertiary-dark}"
    textColor: "{colors.on-surface-dark}"
  banner-inverse:
    backgroundColor: "{colors.surface-inverse}"
    textColor: "{colors.on-surface-inverse}"
    padding: "{spacing.xl}"
  banner-inverse-dark:
    backgroundColor: "{colors.surface-inverse-dark}"
    textColor: "{colors.on-surface-inverse-dark}"
  divider:
    backgroundColor: "{colors.outline-subtle}"
  divider-dark:
    backgroundColor: "{colors.outline-subtle-dark}"
  button-size-sm:
    height: "32px"
    rounded: "{rounded.sm}"
    typography: "{typography.label-sm}"
  button-size-md:
    height: "40px"
    rounded: "{rounded.sm}"
    typography: "{typography.label-md}"
  button-size-lg:
    height: "55px"
    rounded: "{rounded.sm}"
    typography: "{typography.body-md}"
  button-disabled:
    backgroundColor: "{colors.surface-disabled}"
    textColor: "{colors.on-surface-disabled}"
    rounded: "{rounded.sm}"
  button-disabled-dark:
    backgroundColor: "{colors.surface-disabled-dark}"
    textColor: "{colors.on-surface-disabled-dark}"
  input:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    borderColor: "{colors.outline}"
    rounded: "{rounded.sm}"
    padding: "{spacing.lg}"
    typography: "{typography.input}"
  input-dark:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.on-surface-dark}"
    borderColor: "{colors.outline-dark}"
  input-hover:
    borderColor: "{colors.outline-hover}"
  input-hover-dark:
    borderColor: "{colors.outline-hover-dark}"
  input-focus:
    borderColor: "{colors.outline-active}"
  input-focus-dark:
    borderColor: "{colors.outline-active-dark}"
  input-error:
    backgroundColor: "{colors.surface-danger}"
    textColor: "{colors.on-danger}"
    borderColor: "{colors.outline-danger}"
  input-error-dark:
    textColor: "{colors.on-danger-dark}"
    borderColor: "{colors.outline-danger-dark}"
  input-disabled:
    backgroundColor: "{colors.surface-disabled}"
    textColor: "{colors.on-surface-disabled}"
  input-disabled-dark:
    backgroundColor: "{colors.surface-disabled-dark}"
    textColor: "{colors.on-surface-disabled-dark}"
  input-placeholder:
    textColor: "{colors.placeholder}"
  input-placeholder-dark:
    textColor: "{colors.placeholder-dark}"
  input-label:
    textColor: "{colors.on-surface}"
    typography: "{typography.label-md}"
  input-label-dark:
    textColor: "{colors.on-surface-dark}"
  input-help-text:
    textColor: "{colors.on-surface-secondary}"
    typography: "{typography.body-xs}"
  input-help-text-dark:
    textColor: "{colors.on-surface-secondary-dark}"
  modal:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.md}"
    padding: "{spacing.3xl}"
  modal-dark:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.on-surface-dark}"
  alert-success:
    backgroundColor: "{colors.surface-success}"
    textColor: "{colors.on-success}"
    borderColor: "{colors.outline-success}"
    rounded: "{rounded.sm}"
    padding: "{spacing.xl}"
  alert-success-dark:
    textColor: "{colors.on-success-dark}"
  alert-danger:
    backgroundColor: "{colors.surface-danger}"
    textColor: "{colors.on-danger}"
    borderColor: "{colors.outline-danger}"
    rounded: "{rounded.sm}"
    padding: "{spacing.xl}"
  alert-danger-dark:
    textColor: "{colors.on-danger-dark}"
    borderColor: "{colors.outline-danger-dark}"
  alert-warning:
    backgroundColor: "{colors.surface-warning}"
    textColor: "{colors.on-warning}"
    borderColor: "{colors.outline-warning}"
    rounded: "{rounded.sm}"
    padding: "{spacing.xl}"
  alert-warning-dark:
    textColor: "{colors.on-warning-dark}"
    borderColor: "{colors.outline-warning-dark}"
  alert-info:
    backgroundColor: "{colors.surface-info}"
    textColor: "{colors.on-info}"
    borderColor: "{colors.outline-info}"
    rounded: "{rounded.sm}"
    padding: "{spacing.xl}"
  alert-info-dark:
    textColor: "{colors.on-info-dark}"
  chart-series-1:
    backgroundColor: "{colors.chart-1}"
  chart-series-2:
    backgroundColor: "{colors.chart-2}"
  chart-series-3:
    backgroundColor: "{colors.chart-3}"
---

<!--
  Do not edit this file directly.

  The YAML front matter is generated from the design tokens in
  packages/design-tokens/tokens/ (curated by design-md/designMdConfig.js).
  The prose below is authored in packages/design-tokens/design-md/prose.md.

  Regenerate with `pnpm build-tokens`.
-->

# Hyphen

## Overview

Hyphen is a product design system for operational software — feature flagging,
environment management, access control, and the dashboards that sit on top of
them. The people using it are engineers and operators doing focused work, often
for hours at a time, often on dense screens full of state.

That shapes the whole system. The UI is **quiet, neutral, and information-dense**.
Near-monochrome greys carry the interface so that the only saturated color on a
screen is something that matters: a destructive action, a failed deploy, a live
toggle. Chrome recedes; data leads.

The feel is precise rather than playful. Corners are gently rounded, never
pill-shaped. Depth is subtle — a card lifts off the page just enough to read as a
distinct surface. Motion is short and functional. Nothing decorative competes
with the content.

Hyphen's brand palette is vivid — yellows, magentas, cyans, purples — but that
energy lives in marketing surfaces, empty states, and illustration, not in the
working UI. When in doubt, reach for a neutral and let the accent go unused.

Every surface must work in both light and dark themes. Dark is a first-class
mode, not an inversion filter.

## Colors

Color is organized by **role**, not by hue. Prose and code should name the role
(`surface`, `on-surface`, `outline`, `primary`) and let the token carry the
value. Reaching past the semantic layer into the raw palette is how a design
system drifts.

- **Primary** — the near-black ink used for the single most important action on
  a screen, and its inverse in dark mode. Hyphen has no saturated "brand blue"
  button; the primary action is high-contrast neutral.
- **Surface / on-surface** — the page and card backgrounds and the text that sits
  on them, in three steps (`surface`, `surface-secondary`, `surface-tertiary`)
  for building visual layers without shadows.
- **Outline** — borders and dividers, with `outline-subtle` for quiet separators
  and `outline-hover` / `outline-active` for interactive states.
- **Status** — `success`, `danger`, `warning`, `info` each come as a surface, a
  text color, and an outline, so alerts and inline validation stay consistent.
- **Brand** — the nine brand hues, available as backgrounds, text, and borders.
  Use them for marketing, illustration, and identity moments.
- **Chart** — an ordered series palette for data visualization.

### Light and dark

Every token below is a light-theme value. Where the dark theme differs, the same
token exists with a **`-dark` suffix**, and components that change across themes
have a matching `-dark` variant. So `button-primary` and `button-primary-dark`
are the same component in two themes, not two different components.

A token with no `-dark` sibling is theme-invariant — it is deliberately the same
value in both modes. In CSS these map to the `:root.dark` layer shipped by
`@hyphen/hyphen-design-tokens`.

### Naming

Token names here map directly onto Hyphen's CSS custom properties. A component
token like `button-primary-background` corresponds to
`var(--color-background-button-primary)`; `-font` maps to `--color-font-*` and
`-border` to `--color-border-*`.

## Typography

Hyphen sets everything in **Geist**, with **Geist Mono** for code, keys, IDs, and
any value the user might copy. There is no second display face — hierarchy comes
from size and weight, not from mixing families.

- **Display** (`display-sm`–`display-lg`) — marketing and empty states only.
  These sizes have no place in the product UI.
- **Headings** (`h1`–`h6`) — semibold, tight `1.25` line height. The `h1`–`h6`
  levels here are the real output of the `Heading` component's default size map,
  so an `h3` in this file is what `<Heading as="h3">` renders.
- **Body** (`body-xs`–`body-lg`) — regular weight, roomy `1.5` line height for
  readability. `body-md` is the default UI text size.
- **Labels** (`label-sm`, `label-md`) — medium weight, tight line height, for
  buttons, form labels, table headers, and badges.
- **Input** — a slightly tighter `1.15` line height so text sits optically
  centered inside form controls.
- **Code** — Geist Mono at body-small size.

Size and weight do the work of hierarchy. Avoid using color to signal importance
in text; reserve it for status.

## Layout

Spacing follows a **rem-based t-shirt scale** (`2xs` through `10xl`) rather than
a strict multiple-of-N grid. The steps are deliberately close together at the
small end (`2xs` 0.125rem through `xl` 1rem) because dense operational UI needs
fine control over gaps, and they open up quickly at the large end for page-level
rhythm.

`xl` (1rem) is the workhorse: default gap between related controls, default
padding inside compact containers. `3xl` (1.5rem) is the standard card and modal
padding. Anything `5xl` and above is page-level section spacing.

Layout is composed with the `Box` component and its responsive props rather than
ad-hoc CSS. Every spacing, sizing, and color prop accepts either a single value
or a per-breakpoint object (`{ base, tablet, desktop, hd }`), which is how
responsive behavior is expressed throughout the system.

**Breakpoints**

| Token | Value |
| --- | --- |
| `tablet` | `680px` |
| `desktop` | `992px` |
| `hd` | `1280px` |

Breakpoints are min-width and mobile-first: `base` styles apply everywhere, and
each larger breakpoint overrides from that width up.

## Elevation & Depth

Hierarchy comes primarily from **tonal layering** — stacking `surface`,
`surface-secondary`, and `surface-tertiary`, and separating regions with
`outline-subtle`. Reach for a border or a background step before reaching for a
shadow.

Shadows are reserved for surfaces that genuinely float above the page:
dropdowns, popovers, modals, and toasts. They are soft and low-contrast; the
scale climbs with how far the element is meant to read as lifted, not with how
much attention it deserves.

**Shadow scale**

| Token | Value |
| --- | --- |
| `0` | `0 0 #000000` |
| `2xs` | `0 1px 2px 0 rgba(0, 0, 0, 0.06)` |
| `xs` | `0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)` |
| `sm` | `0 2px 5px rgba(0, 0, 0, 0.1), 0 1px 3px rgba(0, 0, 0, 0.06)` |
| `md` | `0 1px 5px 0 rgba(0,0,0,0.05), 0 1px 2px rgba(0,0,0,0.05), 0 4px 6px rgba(0,0,0,0.1)` |
| `lg` | `0 10px 12px -4px rgba(0, 0, 0, 0.1), 0 4px 6px rgba(0, 0, 0, 0.06)` |
| `xl` | `0 16px 20px -4px rgba(0, 0, 0, 0.1), 0 8px 12px rgba(0, 0, 0, 0.05)` |
| `2xl` | `0 20px 40px -5px rgba(0, 0, 0, 0.16)` |

Stacking order is fixed by token so that overlapping surfaces compose
predictably. Never invent a `z-index` — use the scale.

**Stacking order**

| Token | Value |
| --- | --- |
| `0` | `0` |
| `popover` | `9000` |
| `modal` | `8000` |
| `overlay` | `7000` |
| `drawer` | `200` |
| `sticky` | `100` |

## Shapes

Corners are **softly squared**. The default for interactive controls — buttons,
inputs, badges — is `sm` (0.25rem): enough to feel finished, not enough to read
as friendly. Containers that hold content, such as cards, modals, and popovers,
use `md` (0.5rem). The larger steps exist for marketing surfaces and for avatars
and other genuinely circular elements at `3xl`.

Hyphen has no fully-pill shape in its product UI. Mixing radii within a single
composed control — a button group, an input with an attached addon — is the most
common way this goes wrong; the outer shape wins and inner elements square off.

**Border widths**

| Token | Value |
| --- | --- |
| `0` | `0rem` |
| `sm` | `0.0625rem` |
| `md` | `0.125rem` |
| `lg` | `0.25rem` |

Borders are hairline by default (`sm`, 0.0625rem). Heavier widths are for focus
and selection states, not for decoration.

## Components

The component tokens in the front matter describe the atoms that carry Hyphen's
visual identity. They are grouped the way the system actually works:

- **Buttons** — five intents (`primary`, `secondary`, `tertiary`, `danger`,
  `success`), each with `-hover` and `-active` variants, plus three fixed sizes
  (`button-size-sm` 32px, `button-size-md` 40px, `button-size-lg` 55px) and a
  shared disabled treatment. `primary` is high-contrast neutral; `tertiary` is
  transparent until hovered.
- **Badges** — `default`, `secondary`, `danger`, and `outline`.
- **Form controls** — `input` and its `-hover`, `-error`, `-disabled`, and
  `-placeholder` states, plus `input-label` and `input-help-text`, and the
  `checkbox` / `checkbox-checked` pair.
- **Overlays** — `modal`, `popover`, `tooltip`, `toast`, and `toast-error`.
  Tooltips and toasts invert against the page; modals and popovers sit on
  `surface`.
- **Feedback** — `alert-success`, `alert-danger`, `alert-warning`, `alert-info`.
- **Containers** — `page`, `card`, `card-subtle`, and `divider`.
- **Brand and data** — the nine `brand-*` surfaces and the `chart-series-*`
  palette.

These are style contracts, not the full component API. The implementations,
their props, and their accessibility behavior live in
`@hyphen/hyphen-components`, documented at <https://ux.hyphen.ai>. Prefer using a
component from that library over rebuilding one from these tokens.

## Do's and Don'ts

- **Do** use semantic role tokens (`surface`, `on-surface`, `outline`,
  `primary`). **Don't** reach into the raw grey/blue/red palette scales directly
  — that is how themes break.
- **Do** let one primary action own a screen. **Don't** put two `primary`
  buttons in the same view; the second one should be `secondary` or `tertiary`.
- **Do** pair every color token with its `-dark` sibling when a surface can
  appear in both themes. **Don't** ship a component that only works in light
  mode.
- **Do** reach for tonal layering and `outline-subtle` to separate regions.
  **Don't** add a shadow to something that isn't actually floating.
- **Do** use the spacing scale for every gap and pad. **Don't** hardcode pixel
  values — if a gap needs a value the scale doesn't have, the layout is usually
  the thing that's wrong.
- **Do** keep body text at `body-md` or larger and maintain WCAG AA contrast
  (4.5:1 for normal text, 3:1 for large text and UI boundaries). **Don't** use
  `body-xs` for anything a user has to read carefully.
- **Do** reserve saturated color for status and destructive actions. **Don't**
  decorate the working UI with the brand palette.
- **Do** use `Box` and its responsive props for layout. **Don't** write ad-hoc
  CSS that bypasses the token layer.
- **Do** use the `z-index` scale for stacking. **Don't** invent a magic number.
